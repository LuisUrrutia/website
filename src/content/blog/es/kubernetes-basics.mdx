---
title: "Fundamentos de Kubernetes para Desarrolladores"
description: "Conceptos esenciales de Kubernetes que todo desarrollador debería entender."
date: 2026-01-01
category: "DevOps"
tags: ["kubernetes", "devops", "contenedores"]
lang: "es"
translationSlug: "kubernetes-basics"
---

Kubernetes se ha convertido en el estándar para orquestación de contenedores. Estos son los fundamentos que necesitas conocer.

## Conceptos Principales

### Pods

La unidad desplegable más pequeña en Kubernetes. Un pod puede contener uno o más contenedores.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
    - name: app
      image: my-app:1.0.0
      ports:
        - containerPort: 8080
```

### Deployments

Gestionan el estado deseado de tu aplicación, incluyendo réplicas y actualizaciones.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - name: app
          image: my-app:1.0.0
```

### Services

Exponen tus pods a la red, proporcionando endpoints estables.

```yaml
apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
```

## Conclusión

Entender estos conceptos básicos te ayudará a trabajar efectivamente con Kubernetes en cualquier entorno.
