---
import { DEFAULT_LANGUAGE } from "../i18n/constants";
---

<!-- We should never land on this page, as there should be a redirection for each language. 
 However, in case it's not configured, we will redirect using HTML.
--><!doctype html>
<html lang={DEFAULT_LANGUAGE}>
	<head>
		<meta http-equiv="refresh" content={`0;url=/${DEFAULT_LANGUAGE}/`} />
	</head>
</html>
