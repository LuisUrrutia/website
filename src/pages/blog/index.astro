---
import BlogPage from "@/views/BlogPage.astro";
import { getPaginatedBlogPosts } from "@/data/blog";

const { posts, currentPage, totalPages } = await getPaginatedBlogPosts("en");
---

<BlogPage posts={posts} currentPage={currentPage} totalPages={totalPages} />
